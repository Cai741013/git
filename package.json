{
  "name": "feishu-solution-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node --env-file-if-exists=.env --use-env-proxy server.js",
    "check": "node --check app.js && node --check server.js"
  }
}
